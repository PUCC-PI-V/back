info:
  name: Criar calculo orçamento
  type: http
  seq: 10

http:
  method: POST
  url: http://localhost:8000/
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Login
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8000/admin/login
  body:
    type: json
    data: |-
      {
        "email": "admin@viboraink.com",
        "password": "admin123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

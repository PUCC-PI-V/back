info:
  name: Pegar todos os orçamentos
  type: http
  seq: 8

http:
  method: GET
  url: http://localhost:8000/budget/submit
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

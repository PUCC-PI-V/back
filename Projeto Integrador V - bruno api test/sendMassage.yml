info:
  name: sendMassage
  type: http
  seq: 2

http:
  method: POST
  url: http://127.0.0.1:8000/ia/prompt
  body:
    type: json
    data: |-
      {
        "prompt":"vale a pena pegar um trabalho de cu peludo?"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
